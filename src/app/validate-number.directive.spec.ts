import { ValidateNumberDirective } from './validate-number.directive';

describe('ValidateNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateNumberDirective();
    expect(directive).toBeTruthy();
  });
});
